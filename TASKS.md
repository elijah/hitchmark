# Tasks

## Active
- [ ] **Push workflow/cross-compile commit to origin/master** - push commit `53711be` with PAT that has `workflow` scope
- [ ] **Run a tagged release dry run in GitHub Actions** - push next `v*` tag and verify `build-windows-tray` publishes `hitchmark-tray-windows-x86_64.zip`
- [ ] **Finish Windows tray v0.6.0 polish items** - global hotkey, tray notifications, startup-on-login entry, MSI packaging, and tray-focused tests

## Waiting On
- [ ] **GitHub token with workflow scope** - for updating `.github/workflows/release.yml` on protected remotes

## Someday

## Done
